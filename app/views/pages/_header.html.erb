<nav class="navbar fixed-top navbar-expand-lg bg-body-tertiary" data-bs-theme="dark" potision="fixed">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path%>">
        <%= image_tag("lr_logo.png", :class => "lr_logo")%>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <%# <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Home</a>
        </li> %>
        <li class="nav-item">
          <a class="nav-link" href="<%= about_path %>">About</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Articles
          </a>
          <ul class="dropdown-menu">
            <%= link_to "Create new article", new_article_path, class: "dropdown-item"%>
            <%= link_to "View all articles", articles_path, class: "dropdown-item"%>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="#">My Articles</a></li>
          </ul>
        </li>
        <li class="nav-item">
          <a class="nav-link " href="<%= users_path%>">Users</a>
        </li>
      </ul>      
      <%# <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"> %>
        <% if logged_in? %>
            <%= link_to "My Profile", user_path(current_user), {class: "btn btn-outline-info me-md-2"}%>
            <%= button_to "Log out", logout_path, method: :delete, class: "btn btn-outline-info me-md-2" %>
        <% else %>
            <%= link_to "Sign Up", signup_path, {class: "btn btn-outline-info me-md-2"} %>
            <%= link_to "Log In", login_path, {class: "btn btn-outline-info me-md-2"}%>
        <%end%>
      </form>
    </div>
  </div>
</nav>